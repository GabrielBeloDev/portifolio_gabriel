// A page-level affordance (e.g. the blog search box) opens the shared command
// palette by dispatching this on window, so it stays decoupled from the shell.
export const OPEN_PALETTE_EVENT = "open-command-palette";
